(function(){
'use strict';
if(!window.pdfjsLib||!window.XLSX)return;
const $=id=>document.getElementById(id);
const pointInput=$('pointFile'),scheduleInput=$('scheduleFile'),calcBtn=$('calculateBtn'),resetBtn=$('resetBtn');
let pointData=null,scheduleData=null;
const NONWORK=new Set(['F','FER','AF','AB','AL','FF','FC','NC','AE']);
const FLEX=new Set(['D']);
const CODE=/^(?:T\d{1,2}|F|FER|AF|AB|AL|FF|FC|NC|AE|D)$/i;
const MONTHS={JANEIRO:0,FEVEREIRO:1,MARCO:2,MARÇO:2,ABRIL:3,MAIO:4,JUNHO:5,JULHO:6,AGOSTO:7,SETEMBRO:8,OUTUBRO:9,NOVEMBRO:10,DEZEMBRO:11};
const norm=v=>String(v??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase().replace(/[^A-Z0-9 ]/g,' ').replace(/\s+/g,' ').trim();
const iso=(y,m,d)=>`${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
const br=v=>{const m=String(v).match(/(\d{2})\/(\d{2})\/(\d{4})/);return m?`${m[3]}-${m[2]}-${m[1]}`:null};
const fmtDate=v=>v?v.split('-').reverse().join('/'):'—';
const mins=t=>{const [h,m]=String(t).split(':').map(Number);return h*60+m};
const diff=(a,b)=>{const d=Math.abs(mins(a)-mins(b));return Math.min(d,1440-d)};
const pct=(v,d=1)=>`${(v*100).toFixed(d).replace('.',',')}%`;
function status(id,text,ok){const e=$(id);e.textContent=text;e.classList.remove('muted','ok','error');e.classList.add(ok?'ok':'error')}
function enable(){calcBtn.disabled=!(pointData&&scheduleData)}
function lev(a,b){const m=a.length,n=b.length,d=Array.from({length:m+1},(_,i)=>[i]);for(let j=1;j<=n;j++)d[0][j]=j;for(let i=1;i<=m;i++)for(let j=1;j<=n;j++)d[i][j]=Math.min(d[i-1][j]+1,d[i][j-1]+1,d[i-1][j-1]+(a[i-1]===b[j-1]?0:1));return d[m][n]}
function sim(a,b){return 1-lev(a,b)/Math.max(1,a.length,b.length)}
function tokenSim(a,b){const A=new Set(a.split(' ').filter(Boolean)),B=new Set(b.split(' ').filter(Boolean)),u=new Set([...A,...B]).size||1;return [...A].filter(x=>B.has(x)).length/u}
function detectStore(text){const m=String(text).match(/\bML\s*0*(\d{1,3})\b/i);if(m)return `ML${String(+m[1]).padStart(2,'0')}`;const l=String(text).match(/\bLOJA\s*0*(\d{1,3})\b/i);return l?`ML${String(+l[1]).padStart(2,'0')}`:null}
function detectMonthYear(text){const n=norm(text);for(const [name,month] of Object.entries(MONTHS)){let m=n.match(new RegExp(`\\b${norm(name)}\\s+(20\\d{2})\\b`));if(m)return{month,year:+m[1]};m=n.match(new RegExp(`\\b(20\\d{2})\\s+${norm(name)}\\b`));if(m)return{month,year:+m[1]}}return{month:null,year:null}}
function buildDates(days,month,year){if(month==null||!year||!days.length)return[];let m=month,y=year,p=days[0];return days.map((d,i)=>{if(i&&d<p){m++;if(m>11){m=0;y++}}p=d;return iso(y,m,d)})}
function parseLegendText(text){const out=new Map(),s=String(text).replace(/\r/g,'');for(const re of [/\b(T\d{1,2})\s*(?:\||-|:)?\s*([0-2]\d:[0-5]\d)\s*(?:A|À|AS|ÀS|-)\s*([0-2]\d:[0-5]\d)/gi,/\b(T\d{1,2})\b[^\n]{0,32}?([0-2]\d:[0-5]\d)[^\n]{0,12}?([0-2]\d:[0-5]\d)/gi])for(const m of s.matchAll(re)){const k=norm(m[1]);if(!out.has(k))out.set(k,{start:m[2],end:m[3]})}return out}
function mergeTurns(a,b){for(const [k,v] of b)if(!a.has(k))a.set(k,v);return a}
function makeRows(items,tol=2.5){const rows=[];for(const it of items.slice().sort((a,b)=>b.y-a.y||a.x-b.x)){let r=rows.find(x=>Math.abs(x.y-it.y)<tol);if(!r){r={y:it.y,items:[]};rows.push(r)}r.items.push(it)}for(const r of rows){r.items.sort((a,b)=>a.x-b.x);r.text=r.items.map(x=>x.text).join(' ')}rows.sort((a,b)=>b.y-a.y);return rows}
async function pdfTextPages(file){const bytes=new Uint8Array(await file.arrayBuffer()),pdf=await pdfjsLib.getDocument({data:bytes}).promise,pages=[];for(let p=1;p<=pdf.numPages;p++){const page=await pdf.getPage(p),tc=await page.getTextContent(),items=tc.items.filter(i=>i.str&&i.str.trim()).map(i=>({text:i.str.trim(),x:i.transform[4],y:i.transform[5],w:i.width||0})),rows=makeRows(items);pages.push({page:p,pdfPage:page,items,rows,text:rows.map(r=>r.text).join('\n'),source:'text'})}return pages}
async function ocrPage(page){if(!window.Tesseract)throw new Error('PDF sem camada de texto. OCR não carregado; verifique a internet.');const vp=page.getViewport({scale:2}),canvas=document.createElement('canvas');canvas.width=Math.ceil(vp.width);canvas.height=Math.ceil(vp.height);await page.render({canvasContext:canvas.getContext('2d'),viewport:vp}).promise;const r=await Tesseract.recognize(canvas,'por+eng',{logger:m=>{if(m.status==='recognizing text')status('scheduleStatus',`OCR da escala: ${Math.round((m.progress||0)*100)}%`,false)}});const words=(r.data.words||[]).filter(w=>w.text&&w.confidence>=45).map(w=>({text:w.text.trim(),x:w.bbox.x0/2,y:(vp.height-w.bbox.y1)/2,w:(w.bbox.x1-w.bbox.x0)/2}));const rows=makeRows(words,5);return{page:1,pdfPage:page,items:words,rows,text:rows.map(x=>x.text).join('\n'),source:'ocr'} }
async function schedulePdfPages(file){let pages=await pdfTextPages(file);const useful=pages.reduce((n,p)=>n+p.items.length,0),hasGrid=pages.some(p=>/\bNOME\b/i.test(p.text)&&/\bCARGO\b/i.test(p.text));if(useful>=80&&hasGrid)return pages;const o=[];for(const p of pages)o.push(await ocrPage(p.pdfPage));return o}
function parsePoint(pages){const out={type:'point',store:null,periodStart:null,periodEnd:null,employees:new Map(),quality:{rows:0,marks:0}};let cur=null,daily=false;for(const pg of pages)for(const r of pg.rows){const line=r.text,pm=line.match(/Espelho\s+do\s+Ponto\s+(\d{2}\/\d{2}\/\d{4})\s*-\s*(\d{2}\/\d{2}\/\d{4})/i);if(pm){out.periodStart=br(pm[1]);out.periodEnd=br(pm[2])}if(/Matr[ií]cula\s*:/i.test(line)&&/Nome\s*:/i.test(line)){const name=(line.match(/Nome\s*:\s*(.*?)(?=\s+(?:Chapa|Admiss[aã]o)\s*:|$)/i)||[])[1],reg=(line.match(/Matr[ií]cula\s*:\s*(.*?)(?=\s+Nome\s*:|$)/i)||[])[1];if(name){const key=norm(name);if(!out.employees.has(key))out.employees.set(key,{name:name.trim(),key,registration:(reg||'').trim(),cargo:'NÃO IDENTIFICADO',store:null,days:new Map()});cur=out.employees.get(key);daily=false}continue}if(!cur)continue;const cargo=(line.match(/^Fun[cç][aã]o\s*:\s*(?:\d+\s*-\s*)?(.*?)(?=\s+C\.C\.|\s+CPF\s*:|$)/i)||[])[1];if(cargo)cur.cargo=norm(cargo);const ml=(line.match(/\bML\s*0*(\d{1,3})\b/i)||[])[1];if(ml){cur.store=`ML${String(+ml).padStart(2,'0')}`;if(!out.store)out.store=cur.store}if(/^Data\s+Dia\s+1a\s+E\./i.test(line)){daily=true;continue}if(/^Hor[aá]rios\b/i.test(line)||/^(?:C[oó]digo\s+Descri[cç][aã]o|Assinatura)/i.test(line)){daily=false;continue}if(!daily)continue;const dm=line.match(/^(\d{2}\/\d{2}\/\d{4})\b/);if(!dm)continue;const marks=[...line.matchAll(/\b([0-2]\d:[0-5]\d)\s*(?:O|I|P)\b/g)].map(m=>m[1]),date=br(dm[1]);cur.days.set(date,{date,firstEntry:marks[0]||null,marks,raw:line});out.quality.rows++;out.quality.marks+=marks.length}if(!out.employees.size)throw new Error('Espelho: funcionários não reconhecidos.');if(!out.periodStart||!out.periodEnd)throw new Error('Espelho: período não reconhecido.');if(!out.store)throw new Error('Espelho: loja não reconhecida.');return out}
function headerCandidates(page){return page.rows.filter(r=>/\bNOME\b/i.test(r.text)&&/\bCARGO\b/i.test(r.text))}
function parseSchedulePdf(pages){const full=pages.map(p=>p.text).join('\n'),out={type:'schedule',source:pages.some(p=>p.source==='ocr')?'pdf-ocr':'pdf',store:null,periodStart:null,periodEnd:null,employees:new Map(),turns:new Map(),quality:{source:pages.some(p=>p.source==='ocr')?'ocr':'text',headers:0,daySlots:0,codeCells:0,rows:0}};for(const p of pages){const top=p.rows.slice(0,Math.max(8,Math.ceil(p.rows.length*.22))).map(r=>r.text).join(' '),s=detectStore(top);if(s&&!out.store)out.store=s;mergeTurns(out.turns,parseLegendText(p.rows.slice(0,Math.max(12,Math.ceil(p.rows.length*.5))).map(r=>r.text).join('\n')))}if(!out.store)out.store=detectStore(full);let my={month:null,year:null};for(const p of pages){const top=p.rows.slice(0,Math.max(10,Math.ceil(p.rows.length*.25))).map(r=>r.text).join(' '),x=detectMonthYear(top);if(x.month!=null){my=x;break}}if(my.month==null)throw new Error('Escala PDF: mês/ano não reconhecido no cabeçalho.');for(const p of pages){for(const hdr of headerCandidates(p)){out.quality.headers++;const nameIt=hdr.items.find(i=>norm(i.text)==='NOME')||hdr.items[0],cargoIt=hdr.items.find(i=>norm(i.text)==='CARGO');if(!cargoIt)continue;let days=p.items.filter(i=>Math.abs(i.y-hdr.y)<18&&/^\d{1,2}$/.test(i.text)&&+i.text>=1&&+i.text<=31&&i.x>cargoIt.x+20).sort((a,b)=>a.x-b.x),uniq=[];for(const d of days)if(!uniq.some(x=>Math.abs(x.x-d.x)<3))uniq.push(d);days=uniq;if(days.length<20)continue;out.quality.daySlots=Math.max(out.quality.daySlots,days.length);const dates=buildDates(days.map(d=>+d.text),my.month,my.year),first=days[0].x,last=days.at(-1).x,spacing=days.length>1?(last-first)/(days.length-1):14,tol=Math.max(6,spacing*.55);for(const row of p.rows){if(row.y>=hdr.y-3)continue;const codes=row.items.filter(i=>i.x>=first-tol&&i.x<=last+tol&&CODE.test(norm(i.text)));if(codes.length<5)continue;const name=row.items.filter(i=>i.x<cargoIt.x-3).map(i=>i.text).join(' ').trim();if(!name||norm(name)==='NOME')continue;const cargo=row.items.filter(i=>i.x>=cargoIt.x-3&&i.x<first-tol).map(i=>i.text).join(' ').trim(),key=norm(name),emp=out.employees.get(key)||{name,key,cargo:norm(cargo)||'NÃO IDENTIFICADO',days:new Map()};for(const ci of codes){let bi=-1,bd=Infinity;days.forEach((d,j)=>{const z=Math.abs(d.x-ci.x);if(z<bd){bd=z;bi=j}});if(bi>=0&&bd<=tol&&dates[bi]){const code=norm(ci.text),turn=out.turns.get(code);emp.days.set(dates[bi],{date:dates[bi],code,start:turn?.start||null});out.quality.codeCells++}}if(emp.days.size>=5){out.employees.set(key,emp);out.quality.rows++}}}}
if(!out.employees.size)throw new Error('Escala PDF: matriz Nome × Dias não reconhecida.');const dates=[...out.employees.values()].flatMap(e=>[...e.days.keys()]).sort();out.periodStart=dates[0]||null;out.periodEnd=dates.at(-1)||null;return out}
function cellText(v){return v==null?'':v instanceof Date?v.toISOString():String(v).trim()}
function serial(v){if(typeof v!=='number'||v<30000)return null;const o=XLSX.SSF.parse_date_code(v);return o?iso(o.y,o.m-1,o.d):null}
function cellDate(v){if(v instanceof Date&&!isNaN(v))return iso(v.getFullYear(),v.getMonth(),v.getDate());return serial(v)||br(v)}
function safeRows(ws,maxRows=520,maxCols=430,raw=true){if(!ws||!ws['!ref'])return[];const rg=XLSX.utils.decode_range(ws['!ref']);const er=Math.min(rg.e.r,rg.s.r+maxRows-1),ec=Math.min(rg.e.c,rg.s.c+maxCols-1),range={s:rg.s,e:{r:er,c:ec}};return XLSX.utils.sheet_to_json(ws,{header:1,defval:'',raw,range})}
function locateGrid(rows){let best=null;for(let r=0;r<Math.min(rows.length,120);r++)for(let c=0;c<Math.min((rows[r]||[]).length,80);c++)if(norm(cellText(rows[r][c]))==='NOME'){let cargo=null;for(let rr=Math.max(0,r-2);rr<=Math.min(rows.length-1,r+2)&&!cargo;rr++)for(let cc=0;cc<Math.min((rows[rr]||[]).length,80);cc++)if(norm(cellText(rows[rr][cc]))==='CARGO'){cargo={r:rr,c:cc};break}if(cargo){const score=100-Math.abs(cargo.r-r)*5;if(!best||score>best.score)best={nameRow:r,nameCol:c,cargoRow:cargo.r,cargoCol:cargo.c,score}}}return best}
function bestDates(rows,g){let best=null;for(let r=Math.max(0,Math.min(g.nameRow,g.cargoRow)-5);r<=Math.min(rows.length-1,Math.max(g.nameRow,g.cargoRow)+5);r++){const cs=[];let real=0;for(let c=Math.max(g.nameCol,g.cargoCol)+1;c<Math.min((rows[r]||[]).length,430);c++){const v=rows[r][c],d=cellDate(v),n=+cellText(v);if(d){cs.push({c,date:d});real++}else if(Number.isInteger(n)&&n>=1&&n<=31)cs.push({c,day:n})}const sc=real*5+cs.length;if(!best||sc>best.score)best={r,candidates:cs,realDates:real,score:sc}}return best}
function workbookLegend(wb,preferred){const out=new Map(),names=[preferred,'Escala Mensal','Escala Ponto','Andar no Tempo',...wb.SheetNames].filter((x,i,a)=>x&&a.indexOf(x)===i);for(const n of names){const ws=wb.Sheets[n];if(!ws)continue;for(const row of safeRows(ws,220,180,false))for(const v of row){const s=String(v||'');if(/\bT\d{1,2}\b/i.test(s)&&/[0-2]\d:[0-5]\d/.test(s))mergeTurns(out,parseLegendText(s))}if(out.size>=30)break}return out}
function workbookStore(wb,preferred){for(const n of [preferred,'Escala Mensal','Escala Ponto','Configuração']){const ws=wb.Sheets[n];if(!ws)continue;const t=safeRows(ws,20,80,false).map(r=>r.join(' | ')).join('\n'),s=detectStore(t);if(s)return s}return null}
function parseScheduleWorkbook(buffer){const wb=XLSX.read(buffer,{type:'array',cellDates:true,cellFormula:true,dense:false}),priority=['Andar no Tempo','Escala Ponto','Escala Mensal'],candidates=[];for(const n of [...priority,...wb.SheetNames].filter((x,i,a)=>a.indexOf(x)===i)){const ws=wb.Sheets[n];if(!ws)continue;const rows=safeRows(ws,520,430,true);if(!rows.length)continue;const g=locateGrid(rows);if(!g)continue;const dr=bestDates(rows,g);if(!dr||dr.candidates.length<20)continue;let codes=0;for(const row of rows.slice(Math.max(g.nameRow,g.cargoRow)+1,Math.max(g.nameRow,g.cargoRow)+120))for(const v of row)if(CODE.test(norm(cellText(v))))codes++;const pri=n==='Andar no Tempo'?50000:n==='Escala Ponto'?10000:n==='Escala Mensal'?5000:0,score=pri+dr.realDates*100+dr.candidates.length*10+codes;candidates.push({sheetName:n,rows,g,dr,score})}candidates.sort((a,b)=>b.score-a.score);const ch=candidates[0];if(!ch)throw new Error('Escala Excel: aba operacional não reconhecida.');const out={type:'schedule',source:'excel',sheet:ch.sheetName,store:workbookStore(wb,ch.sheetName),periodStart:null,periodEnd:null,employees:new Map(),turns:workbookLegend(wb,ch.sheetName),quality:{sheet:ch.sheetName,realDates:ch.dr.realDates,daySlots:ch.dr.candidates.length,rows:0,codeCells:0}};let cols=ch.dr.candidates;if(!cols.some(x=>x.date)){const top=ch.rows.slice(0,15).map(r=>r.join(' ')).join(' '),my=detectMonthYear(top),ds=buildDates(cols.map(x=>x.day),my.month,my.year);cols=cols.map((x,i)=>({c:x.c,date:ds[i]}))}else cols=cols.filter(x=>x.date);let blanks=0;for(let r=Math.max(ch.g.nameRow,ch.g.cargoRow)+1;r<ch.rows.length;r++){const row=ch.rows[r]||[],name=cellText(row[ch.g.nameCol]);if(!name){if(++blanks>12)break;continue}blanks=0;const key=norm(name);if(!key||/^(TOTAL|LEGENDA|GERENCIAL|CONSOLIDADO|MOTOR DE LANCAMENTOS)$/.test(key))continue;const days=[];for(const d of cols){const code=norm(cellText(row[d.c]));if(CODE.test(code))days.push({date:d.date,code})}if(days.length<5)continue;const emp={name:name.trim(),key,cargo:norm(cellText(row[ch.g.cargoCol]))||'NÃO IDENTIFICADO',days:new Map()};for(const x of days){const t=out.turns.get(x.code);emp.days.set(x.date,{date:x.date,code:x.code,start:t?.start||null});out.quality.codeCells++}out.employees.set(key,emp);out.quality.rows++}if(!out.store)throw new Error('Escala Excel: loja não reconhecida.');if(!out.employees.size)throw new Error('Escala Excel: colaboradores não reconhecidos.');const ds=[...out.employees.values()].flatMap(e=>[...e.days.keys()]).sort();out.periodStart=ds[0]||null;out.periodEnd=ds.at(-1)||null;return out}
function matchEmp(s,points,used){if(points.has(s.key)&&!used.has(s.key))return{emp:points.get(s.key),key:s.key,mode:'exato',confidence:1};const pref=[];for(const[k,p]of points){if(used.has(k))continue;const len=Math.min(k.length,s.key.length);if(len>=12&&(k.startsWith(s.key)||s.key.startsWith(k)))pref.push({emp:p,key:k,mode:'truncado',confidence:len/Math.max(k.length,s.key.length)})}if(pref.length===1)return pref[0];const a=[];for(const[k,p]of points){if(used.has(k))continue;const sc=sim(s.key,k)*.7+tokenSim(s.key,k)*.3;a.push({emp:p,key:k,mode:'aproximado',confidence:sc})}a.sort((x,y)=>y.confidence-x.confidence);return a[0]&&a[0].confidence>=.95&&(!a[1]||a[0].confidence-a[1].confidence>=.05)?a[0]:null}
function pointMarks(p,s,e){let n=0;for(const emp of p.employees.values())for(const[d,x]of emp.days)if(d>=s&&d<=e)n+=x.marks.length;return n}
function renderConfidence(v,final=true){v=Math.max(0,Math.min(100,v));$('analysisArea').classList.remove('hidden');$('confidencePercent').textContent=`${v.toFixed(final?1:0).replace('.',',')}%`;$('confidenceBar').style.width=`${v}%`;const b=$('confidenceBadge');b.className='confidence-badge '+(v>=97?'high':v>=90?'medium':'low');b.textContent=v>=97?'Muito alta':v>=90?'Alta':'Revisar'}
function renderPre(){if(!pointData&&!scheduleData)return;$('analysisArea').classList.remove('hidden');$('resultCard').classList.add('hidden');let q=0;if(pointData?.store)q+=10;if(pointData?.periodStart)q+=10;if(scheduleData?.store)q+=10;if(scheduleData?.periodStart)q+=10;if(scheduleData?.turns?.size>=29)q+=20;if(scheduleData?.employees?.size)q+=20;if(pointData&&scheduleData&&pointData.store===scheduleData.store)q+=10;if(pointData&&scheduleData&&scheduleData.periodStart<=pointData.periodStart&&scheduleData.periodEnd>=pointData.periodEnd)q+=10;renderConfidence(q,false);$('metaSource').textContent=scheduleData?(scheduleData.source==='excel'?'Excel/XLSM':scheduleData.source==='pdf-ocr'?'PDF + OCR':'PDF com texto'):'—';$('metaStore').textContent=scheduleData?.store||pointData?.store||'—';$('metaPeriod').textContent=pointData?`${fmtDate(pointData.periodStart)} a ${fmtDate(pointData.periodEnd)}`:'—';$('metaTurns').textContent=scheduleData?String(scheduleData.turns.size):'—';$('metaPointMarks').textContent=pointData?String(pointMarks(pointData,pointData.periodStart,pointData.periodEnd)):'—';$('metaCoverage').textContent='—'}
function calculate(){if(!pointData||!scheduleData)return;$('analysisArea').classList.remove('hidden');$('resultCard').classList.add('hidden');if(pointData.store!==scheduleData.store)throw new Error(`Lojas diferentes: ${pointData.store} × ${scheduleData.store}.`);const start=pointData.periodStart,end=pointData.periodEnd;if(!scheduleData.periodStart||scheduleData.periodStart>start||scheduleData.periodEnd<end)throw new Error(`A escala não cobre ${fmtDate(start)} a ${fmtDate(end)}.`);const used=new Set(),cargoStats=new Map(),unmatched=[],usedTurns=new Set();let matched=0,trunc=0,approx=0,dev=0,nc=0,total=0,unresolved=0;for(const s of scheduleData.employees.values()){const m=matchEmp(s,pointData.employees,used);if(!m){unmatched.push(s.name);continue}used.add(m.key);matched++;if(m.mode==='truncado')trunc++;if(m.mode==='aproximado')approx++;const cargo=m.emp.cargo!=='NÃO IDENTIFICADO'?m.emp.cargo:s.cargo;if(!cargoStats.has(cargo))cargoStats.set(cargo,{days:0,deviations:0,nc:0});const cs=cargoStats.get(cargo);for(const[date,sd]of s.days){if(date<start||date>end)continue;const pd=m.emp.days.get(date);if(!pd||!pd.marks.length)continue;cs.days++;if(NONWORK.has(sd.code)){nc++;cs.nc++;total+=pd.marks.length;continue}if(/^T\d{1,2}$/.test(sd.code)){usedTurns.add(sd.code);if(!sd.start){unresolved++;continue}total+=pd.marks.length;if(pd.firstEntry&&diff(sd.start,pd.firstEntry)>90){dev++;cs.deviations++}}}}
if(unresolved)throw new Error(`${unresolved} dia(s) usam turno sem horário reconhecido.`);const all=pointMarks(pointData,start,end),coverage=all?total/all:0,pointOnly=[...pointData.employees.entries()].filter(([k])=>!used.has(k)).map(([,e])=>({name:e.name,marks:[...e.days.entries()].reduce((n,[d,x])=>n+(d>=start&&d<=end?x.marks.length:0),0)})).filter(x=>x.marks>0),people=[...pointData.employees.values()].filter(e=>[...e.days.entries()].some(([d,x])=>d>=start&&d<=end&&x.marks.length)).length,nameCov=people?Math.min(1,matched/people):1,turnCov=usedTurns.size?[...usedTurns].filter(t=>scheduleData.turns.has(t)).length/usedTurns.size:1,parseQ=scheduleData.source==='excel'?(scheduleData.quality.realDates>=300?1:.96):(scheduleData.quality.source==='text'?1:.92),confidence=Math.max(0,Math.min(100,100*(.35*coverage+.25*nameCov+.20*turnCov+.20*parseQ)-approx*1.2));renderConfidence(confidence,true);$('metaSource').textContent=scheduleData.source==='excel'?'Excel/XLSM':scheduleData.source==='pdf-ocr'?'PDF + OCR':'PDF com texto';$('metaStore').textContent=pointData.store;$('metaPeriod').textContent=`${fmtDate(start)} a ${fmtDate(end)}`;$('metaTurns').textContent=`${scheduleData.turns.size} (${usedTurns.size} usados)`;$('metaPointMarks').textContent=all;$('metaCoverage').textContent=pct(coverage);$('causeSummary').innerHTML=[`<div class="cause-item ${dev?'attention':''}"><strong>${dev} desvios acima de 90 min</strong><span>Entrada fora da tolerância.</span></div>`,`<div class="cause-item ${nc?'critical':''}"><strong>${nc} folgas/ausências com marcação</strong><span>F, FER, AF, AB, AL, FF, FC, NC ou AE.</span></div>`,pointOnly.length?`<div class="cause-item attention"><strong>${pointOnly.length} pessoa(s) apenas no ponto</strong><span>${pointOnly.reduce((n,x)=>n+x.marks,0)} marcação(ões) sem linha conciliada na escala.</span></div>`:'',unmatched.length?`<div class="cause-item"><strong>${unmatched.length} pessoa(s) apenas na escala</strong><span>Sem correspondência no espelho.</span></div>`:''].join('');const cr=[...cargoStats.entries()].map(([cargo,v])=>({cargo,...v,occ:v.deviations+v.nc})).filter(x=>x.occ).sort((a,b)=>b.occ-a.occ).slice(0,5);$('cargoSummary').innerHTML=cr.length?cr.map(x=>`<div class="cargo-item"><strong>${x.cargo}</strong><span>${x.occ} ocorrência(s) • ${x.deviations} desvio(s) • ${x.nc} folga/ausência</span></div>`).join(''):'<p>Nenhuma ocorrência por cargo.</p>';if(coverage<.98)throw new Error(`Cobertura ${pct(coverage)} abaixo de 98%. Resultado bloqueado.`);if(confidence<92)throw new Error(`Confiabilidade ${pct(confidence/100)} insuficiente. Resultado bloqueado.`);const adh=1-(dev+10*nc)/total;$('resultPercent').textContent=pct(adh,2);$('resultStore').textContent=pointData.store;$('matchedPeople').textContent=`${matched}/${people}`;$('deviations').textContent=dev;$('nonConformities').textContent=nc;$('totalMarks').textContent=total;$('warnings').innerHTML=[`<div>Leitura validada • confiabilidade ${pct(confidence/100)} • cobertura ${pct(coverage)}.</div>`,trunc?`<div>${trunc} nome(s) truncado(s) conciliados por prefixo único.</div>`:'',approx?`<div>${approx} nome(s) conciliado(s) por similaridade controlada.</div>`:''].join('');$('resultCard').classList.remove('hidden')}
async function onPoint(file){pointData=null;$('resultCard').classList.add('hidden');$('pointFileName').textContent=file?.name||'Nenhum arquivo selecionado';if(!file){status('pointStatus','Aguardando arquivo',false);enable();return}try{status('pointStatus','Lendo espelho...',false);const p=await pdfTextPages(file);pointData=parsePoint(p);status('pointStatus',`Reconhecido: ${pointData.employees.size} funcionário(s) • ${pointData.store} • ${pointMarks(pointData,pointData.periodStart,pointData.periodEnd)} marcações`,true);renderPre()}catch(e){status('pointStatus',`Erro: ${e.message}`,false);alert(e.message)}enable()}
async function onSchedule(file){scheduleData=null;$('resultCard').classList.add('hidden');$('scheduleFileName').textContent=file?.name||'Nenhum arquivo selecionado';if(!file){status('scheduleStatus','Aguardando arquivo',false);enable();return}try{status('scheduleStatus','Lendo escala...',false);const n=file.name.toLowerCase();if(n.endsWith('.pdf'))scheduleData=parseSchedulePdf(await schedulePdfPages(file));else if(/\.(xlsx|xlsm|xls)$/.test(n))scheduleData=parseScheduleWorkbook(await file.arrayBuffer());else throw new Error('Formato não suportado.');status('scheduleStatus',`Reconhecida: ${scheduleData.employees.size} funcionário(s) • ${scheduleData.store} • ${scheduleData.turns.size} turnos • ${fmtDate(scheduleData.periodStart)} a ${fmtDate(scheduleData.periodEnd)}`,true);renderPre()}catch(e){status('scheduleStatus',`Erro: ${e.message}`,false);alert(e.message)}enable()}
document.addEventListener('change',e=>{if(e.target===pointInput){e.stopImmediatePropagation();onPoint(pointInput.files?.[0])}else if(e.target===scheduleInput){e.stopImmediatePropagation();onSchedule(scheduleInput.files?.[0])}},true);
document.addEventListener('click',e=>{if(e.target===calcBtn){e.preventDefault();e.stopImmediatePropagation();try{calculate()}catch(err){$('resultCard').classList.add('hidden');alert(err.message)}}else if(e.target===resetBtn){e.preventDefault();e.stopImmediatePropagation();pointInput.value='';scheduleInput.value='';pointData=null;scheduleData=null;$('pointFileName').textContent='Nenhum arquivo selecionado';$('scheduleFileName').textContent='Nenhum arquivo selecionado';$('pointStatus').textContent='Aguardando arquivo';$('scheduleStatus').textContent='Aguardando arquivo';$('analysisArea').classList.add('hidden');enable()}},true);
enable();
})();