(function(){
'use strict';
if(window.__ADERENCIA_RECURRENCE_RC43__)return;window.__ADERENCIA_RECURRENCE_RC43__=true;
const DETAIL_KEY='aderenciaDivergenciasV1',$=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function load(){try{const x=JSON.parse(localStorage.getItem(DETAIL_KEY)||'[]');return Array.isArray(x)?x:[]}catch{return[]}}
function empKey(o){return String(o.registration||'').trim()||String(o.employee||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase().replace(/[^A-Z0-9]/g,'')}
function monthKey(r){return `${r.year}-${String(r.month).padStart(2,'0')}`}
function aggregate(records){const people=new Map(),stores=new Map(),regions=new Map();for(const r of records){const region=window.ADERENCIA_STORE_REGISTRY?.regionOf?.(r.store)||'SEM REGIONAL';if(!stores.has(r.store))stores.set(r.store,{name:r.store,region,occ:0,people:new Set(),repeat:new Set(),months:new Set()});if(!regions.has(region))regions.set(region,{name:region,occ:0,people:new Set(),repeat:new Set(),stores:new Set(),months:new Set()});const s=stores.get(r.store),g=regions.get(region);s.months.add(monthKey(r));g.months.add(monthKey(r));g.stores.add(r.store);for(const o of r.occurrences||[]){const k=empKey(o);if(!k)continue;const pk=`${r.store}|${k}`,p=people.get(pk)||{key:pk,employee:o.employee||'Não identificado',registration:o.registration||'',cargo:o.cargo||'',store:r.store,region,occ:0,months:new Set(),types:new Set()};p.occ++;p.months.add(monthKey(r));p.types.add(o.type);people.set(pk,p);s.occ++;s.people.add(pk);g.occ++;g.people.add(pk)}}for(const p of people.values())if(p.months.size>=2){stores.get(p.store)?.repeat.add(p.key);regions.get(p.region)?.repeat.add(p.key)}return{people:[...people.values()],stores:[...stores.values()],regions:[...regions.values()]}}
function rate(x){return x.people.size?x.repeat.size/x.people.size*100:0}function fmt(v){return `${v.toFixed(1).replace('.',',')}%`}
function tableRows(items,kind){return items.sort((a,b)=>rate(b)-rate(a)||b.occ-a.occ).map(x=>`<div class="rec-row"><strong>${esc(x.name)}</strong>${kind==='store'?`<span>${esc(x.region)}</span>`:`<span>${x.stores.size} loja(s)</span>`}<span>${x.people.size}</span><span>${x.repeat.size}</span><span>${fmt(rate(x))}</span><span>${x.occ}</span></div>`).join('')}
function render(){const box=$('recurrenceContent');if(!box)return;const data=aggregate(load()),rec=data.people.filter(p=>p.months.size>=2).sort((a,b)=>b.months.size-a.months.size||b.occ-a.occ),affected=data.people.length,repeat=rec.length,rr=affected?repeat/affected*100:0;box.innerHTML=`<section class="rec-kpis"><article><span>Pessoas com divergência</span><strong>${affected}</strong></article><article><span>Reincidentes</span><strong>${repeat}</strong></article><article><span>Taxa de reincidência</span><strong>${fmt(rr)}</strong></article><article><span>Ocorrências históricas</span><strong>${data.people.reduce((a,p)=>a+p.occ,0)}</strong></article></section><h3>Reincidência por loja</h3><div class="rec-table"><div class="rec-row head"><span>Loja</span><span>Regional</span><span>Pessoas</span><span>Reincidentes</span><span>Taxa</span><span>Ocorrências</span></div>${tableRows(data.stores,'store')||'<p>Sem histórico suficiente.</p>'}</div><h3>Reincidência por regional</h3><div class="rec-table"><div class="rec-row head"><span>Regional</span><span>Lojas</span><span>Pessoas</span><span>Reincidentes</span><span>Taxa</span><span>Ocorrências</span></div>${tableRows(data.regions,'region')||'<p>Sem histórico suficiente.</p>'}</div><h3>Colaboradores reincidentes</h3><div class="rec-people">${rec.slice(0,100).map(p=>`<div><strong>${esc(p.employee)}</strong><span>${esc(p.store)} • ${esc(p.region)} • ${p.months.size} competências • ${p.occ} ocorrências</span></div>`).join('')||'<p>A reincidência aparecerá quando o mesmo colaborador tiver divergência em duas ou mais competências salvas.</p>'}</div>`}
function install(){const nav=document.querySelector('.view-tabs');if(!nav||$('recurrenceTab'))return;const tab=document.createElement('button');tab.id='recurrenceTab';tab.className='view-tab';tab.type='button';tab.textContent='Recorrência';nav.insertBefore(tab,$('semesterTab'));const view=document.createElement('section');view.id='recurrenceView';view.className='hidden';view.innerHTML='<section class="central-toolbar card"><div><p class="panel-kicker">HISTÓRICO DE DIVERGÊNCIAS</p><h2>Recorrência e reincidência</h2><p>Reincidente = mesmo colaborador, na mesma loja, com divergência em duas ou mais competências salvas.</p></div></section><section id="recurrenceContent" class="card recurrence-content"></section>';document.querySelector('main.shell')?.appendChild(view);const style=document.createElement('style');style.textContent='.recurrence-content{margin-top:10px;padding:16px}.rec-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:18px}.rec-kpis article{border:1px solid #dce4eb;border-radius:10px;padding:12px}.rec-kpis span{display:block;font-size:10px;color:#60748a}.rec-kpis strong{font-size:24px}.rec-table{margin:8px 0 20px}.rec-row{display:grid;grid-template-columns:1.4fr 1.4fr .7fr .8fr .7fr .8fr;gap:8px;align-items:center;padding:9px;border-bottom:1px solid #e8edf2;font-size:11px}.rec-row.head{font-weight:700;background:#f5f8fa;border-radius:8px}.rec-people>div{display:flex;justify-content:space-between;gap:12px;padding:8px;border-bottom:1px solid #edf1f4;font-size:11px}@media(max-width:800px){.rec-kpis{grid-template-columns:1fr 1fr}.rec-row{grid-template-columns:1fr 1fr}.rec-row.head{display:none}.rec-people>div{display:block}}';document.head.appendChild(style);tab.addEventListener('click',()=>{document.querySelectorAll('.view-tab').forEach(x=>x.classList.remove('active'));tab.classList.add('active');['analysisView','batchView','historyView','monitorView','divergenceView','evolutionView','semesterView'].forEach(id=>$(id)?.classList.add('hidden'));view.classList.remove('hidden');render()});window.addEventListener('aderencia:historychange',render);window.addEventListener('aderencia:storeschange',render);window.addEventListener('aderencia:portableloaded',render)}
document.addEventListener('DOMContentLoaded',install);window.ADERENCIA_RECURRENCE={aggregate,render};
})();