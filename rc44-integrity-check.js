(function(){
'use strict';
function run(){const checks=[];const add=(name,ok,detail='')=>checks.push({name,ok:!!ok,detail});add('version',window.ADERENCIA_VERSION==='v1.0 RC46',window.ADERENCIA_VERSION||'ausente');add('history',!!window.ADERENCIA_HISTORY);add('period-api',!!window.ADERENCIA_PERIOD);add('period-coherence',!!window.ADERENCIA_PERIOD_COHERENCE);add('period-alias',window.ADERENCIA_GLOBAL_PERIOD===window.ADERENCIA_PERIOD);add('navigation',!!window.ADERENCIA_NAVIGATION);add('divergence-dashboard',!!window.ADERENCIA_DIVERGENCE_DASHBOARD);add('divergence-capture',!!window.__ADERENCIA_DIVERGENCE_CAPTURE_RC20__);add('pdf-parser',window.ADERENCIA_PDF_PARSER_VERSION==='RC28',window.ADERENCIA_PDF_PARSER_VERSION||'ausente');add('pdf-xlsx-compat',!!window.__ADERENCIA_PDF_XLSX_COMPAT_RC21__);add('recurrence',!!window.ADERENCIA_RECURRENCE);add('store-registry',!!window.ADERENCIA_STORE_REGISTRY);add('ml61-region',window.ADERENCIA_STORE_REGISTRY?.regionOf?.('ML61')==='GUARDIÕES DA CHAMA',window.ADERENCIA_STORE_REGISTRY?.regionOf?.('ML61')||'ausente');try{const sample=[{store:'ML01',month:6,year:2026,occurrences:[{employee:'TESTE',registration:'1',type:'DESVIO_ENTRADA_GT_90'}]},{store:'ML01',month:7,year:2026,occurrences:[{employee:'TESTE',registration:'1',type:'DESVIO_ENTRADA_GT_90'}]}],a=window.ADERENCIA_RECURRENCE?.aggregate?.(sample,'all'),s=a?.stores?.[0];add('recurrence-rule',!!s&&s.repeat.size===1&&s.people.size===1,JSON.stringify({people:s?.people?.size,repeat:s?.repeat?.size}))}catch(e){add('recurrence-rule',false,e.message)}try{const map=window.ADERENCIA_NAVIGATION?.map||{},missing=Object.entries(map).filter(([tab,view])=>document.getElementById(tab)&&!document.getElementById(view));add('navigation-targets',missing.length===0,missing.map(x=>x.join('→')).join(', '))}catch(e){add('navigation-targets',false,e.message)}try{const p=window.ADERENCIA_PERIOD?.get?.(),valid=p&&Number.isInteger(+p.month)&&+p.month>=1&&+p.month<=12&&Number.isInteger(+p.year);add('period-state',valid,p?`${p.month}/${p.year}`:'ausente')}catch(e){add('period-state',false,e.message)}try{const active=window.ADERENCIA_ACTIVE_MODULES||[],dups=active.length-new Set(active).size;add('active-module-duplicates',dups===0,`${active.length} módulos ativos`);add('obsolete-pdf-bridge-not-loaded',!active.includes('divergence-pdf-bridge-rc45.js'))}catch(e){add('active-module-duplicates',false,e.message)}if(window.ADERENCIA_DIVERGENCE_AUDIT)add('divergence-audit',window.ADERENCIA_DIVERGENCE_AUDIT.ok!==false,window.ADERENCIA_DIVERGENCE_AUDIT.ok===false?'inconsistências detectadas':'ok');const ok=checks.every(x=>x.ok);window.ADERENCIA_RC46_HEALTH={ok,checks,checkedAt:new Date().toISOString()};window.ADERENCIA_RC45_HEALTH=window.ADERENCIA_RC46_HEALTH;window.ADERENCIA_RC44_HEALTH=window.ADERENCIA_RC46_HEALTH;if(!ok)console.warn('RC46 integrity issues',checks.filter(x=>!x.ok));else console.info('RC46 integrity OK');return window.ADERENCIA_RC46_HEALTH}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(run,300),{once:true});else setTimeout(run,300);window.ADERENCIA_RUN_RC46_CHECK=run;window.ADERENCIA_RUN_RC45_CHECK=run;window.ADERENCIA_RUN_RC44_CHECK=run;
})();